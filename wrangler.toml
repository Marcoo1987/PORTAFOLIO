name = "jarvis-proxy"
main = "worker/index.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Variables no sensibles (las sensibles van como Secrets con wrangler secret put)
WORKER_ENV = "production"
